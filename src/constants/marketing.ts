/** Launch marketing trust stats (CustomerHome fallback when no active shipments). */
export const TRUST_STATS = {
  onTimePercent: "96.7%",
  deliveriesLabel: "1,100+",
  statesLabel: "13 states",
} as const;
